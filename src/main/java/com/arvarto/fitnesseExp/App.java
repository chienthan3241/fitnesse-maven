package com.arvarto.fitnesseExp;

public class App {
   public static void main(String[] args) {
      // Connection con = null;
      // Statement stm = null;
      // ResultSet rs = null;
      // List<Auftrag> result = new ArrayList<Auftrag>();
      //
      // try {
      // con = DbConnect.getConnection();
      // stm = con.createStatement();
      // String sql = "Select AUFTRAGSTYP, AUFTRAGSTATUS from " + Config.schema
      // + "DQEINGABEDATEIAUFTRAG where DQOBJEKT_ID = '1029301'";
      // System.out.println(sql);
      // rs = stm.executeQuery(sql);
      // while (rs.next()) {
      // result.add(new Auftrag(rs.getString(1), rs.getInt(2)));
      // }
      // for (Auftrag a : result) {
      // System.out.println(a.getName() + "|||" + a.getStatus());
      // }
      //
      // } catch (SQLException e) {
      //
      // } finally {
      // try {
      // if (rs != null) {
      // rs.close(); // close resultSet
      // }
      // if (stm != null) {
      // stm.close(); // close PreparedStatement
      // }
      // if (con != null) {
      // con.close(); // close connection
      // }
      // } catch (SQLException e) {
      // e.printStackTrace();
      // }
      // }
      //
      System.out.println("test");
   }

}
